import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      marginBottom: 25
    },
    filterButton: {
        backgroundColor: '#dcdcdc',
        borderRadius: 10
    },
    SearchBarStyle: {
        flexGrow: 1,
        borderRadius: 10,
        marginRight: 15
    }
});